This is a version controlled repository for LaTeX docuements.
On *nix machines build pdf as follows:

   cd derivations/
   python render_makefile.py
   cd ..
   ./build.sh

It will render your docuement to output/main.pdf by default.
